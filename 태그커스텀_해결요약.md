# 태그 커스텀 "첫 로딩 시 미적용" 문제 — 해결 요약

## 1. 시도한 방법들 (시행착오 포함)

| 순서 | 수정 내용 | 목적 |
|------|-----------|------|
| 1 | **Observer에서 `.mes_text` 자식 변경 시** `data-tag-processed`가 **없을 때만** `shouldApply = true` 설정 | 최초 로딩 시 한 번이라도 적용되게 하기 |
| 2 | **디버그 로그 추가** (`TAG_CUSTOM_DEBUG`, `applyCustomTagFonts`/Observer/이벤트 로그) | 원인 파악용 |
| 3 | **Observer에서 `.mes_text` 내용 변경 시** `data-tag-processed` **유무와 관계없이** 항상 제거 후 `shouldApply = true` | 앱이 나중에 .mes_text를 다시 그릴 때도 재적용 |
| 4 | **MESSAGE_SWIPED 시 `debounceApply` 제거** (처리 표시만 제거) | 스와이프 직후 불필요한 apply 제거(메시지 미로드 시점) |
| 5 | **디버그 로그 제거** (`TAG_CUSTOM_DEBUG = false` 후 전부 제거) | 문제와 무관한 코드 정리 |

---

## 2. 근본 원인

**SillyTavern(또는 메인 앱)이 메시지 DOM을 그리는 타이밍과 Font-Manager가 태그를 적용하는 타이밍이 어긋남.**

- 채팅/메시지가 로드될 때:
  1. 먼저 **`.mes` / `.mes_text` 구조만** 만들거나,
  2. **우리가 `applyCustomTagFonts`로 `innerHTML`을 넣은 뒤**, 앱이 **같은 `.mes_text`를 다시 그리면서** 우리가 적용한 내용을 **덮어씀**.

그래서 “첫 로딩”이나 “스와이프 후 로드” 시점에는 **최종적으로 보이는 DOM이 우리가 적용하기 전 상태**가 됨.

---

## 3. 근본 해결 방법

**`.mes_text`의 내용이 바뀔 때마다(자식 노드 추가/제거 시) 처리 표시를 지우고, 다시 한 번 적용을 예약하는 것.**

- **Observer**에서 `mutation.target`이 `.mes_text`이고 `mutation.addedNodes.length > 0 || mutation.removedNodes.length > 0`일 때:
  - 해당 메시지의 `data-tag-processed` 제거
  - `processedMessages`에서 해당 요소 제거
  - `shouldApply = true` → 150ms 디바운스 후 `applyCustomTagFonts` 실행
- 이렇게 하면 **앱이 내용을 채우거나 덮어쓴 뒤**에도, 그 직후 한 번 더 적용이 돌아가서 **최종 DOM에 태그 커스텀이 적용**됨.

즉, **“.mes_text 내용 변경 시 항상 재적용 예약”**이 근본 해결 방법임.

---

## 4. 문제와 무관했던 시행착오

- **디버그 로그 전부**  
  원인 분석용이었을 뿐, 동작 해결과는 무관. 제거 가능.

- **“처리된 메시지가 다시 그려진다”는 가설 설명**  
  설명용이었고, 실제 수정은 “.mes_text 변경 시 재적용” 하나로 충분.

- **MESSAGE_SWIPED 시 `debounceApply` 제거**  
  근본 원인 해결이 아니라 **스와이프 직후 불필요한 실행/로그를 줄이는 최적화**. 유지 권장.

- **Observer의 “번역문 있으면 재처리” 분기**  
  `.mes_text`의 **자식 변경이 아닌** mutation(예: 번역문만 갱신)일 때 재처리하려는 목적.  
  근본 원인(첫 로딩/덮어쓰기)과는 별개이지만, LLM Translator 사용 시 번역 갱신 시 재적용에 도움 되므로 **유지 권장**.

---

## 5. 정리: 제거한 것 / 유지한 것

### 제거한 것 (문제와 무관한 코드)

- **`TAG_CUSTOM_DEBUG` 상수 및 모든 `if (TAG_CUSTOM_DEBUG) console.log(...)` 블록**  
  시행착오 중 원인 파악용으로만 쓰였고, 해결 로직과 무관하므로 전부 제거.

### 유지한 것 (이유)

| 항목 | 이유 |
|------|------|
| **.mes_text 자식 변경 시 항상 처리 표시 제거 + shouldApply** | 근본 원인 해결의 핵심. 제거하면 첫 로딩/덮어쓰기 시 다시 미적용됨. |
| **MESSAGE_SWIPED 시 debounceApply 호출 안 함** | 스와이프 직후 불필요한 실행/리소스 절약. 유지 권장. |
| **Observer의 “번역문 있으면 재처리” 분기** | LLM Translator 사용 시 번역 갱신만 있을 때도 재적용되게 함. 제거해도 스탠드얼론 동작에는 필수는 아니지만, 있으면 동작이 더 안정적. |
| **새 .mes 추가 감지, 에디터 열림/닫힘 감지** | 기존부터 있던 로직. 새 메시지 추가·에디터 전환 시 적용이 필요한 부분이라 유지. |
| **CHAT_CHANGED 시 forceRefresh, CHARACTER/USER_MESSAGE_RENDERED 시 debounceApply** | 채팅 전환·메시지 렌더 완료 시 적용을 위해 원래부터 쓰이던 보조 수단. 유지. |

---

이 요약은 `태그커스텀_해결요약.md`로 저장됨.
